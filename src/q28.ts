type A = []
type B = string[]
type C = [string] //tuple

// let newArr: B = []

let newArr: C = ['singh']

// string[] says the variable is an array with values of type string (it can be of any size, even empty).
// [string] says the variable is an array of size = 1 and the first entry is a string , fixed length